<tbody id="<%= dom_id user %>">
  <tr>
    <td><%= user.email %></td>
    <td><%= user.points %></td> 
    <td><%= button_to( 'Send points to mail', "/users/#{user.id}/send_points_email" , method: :post, class:"navbar-brand", style:"border: 1px solid #fff; border-radius: 10px; padding: 5px 5px;")  %></td>
    <%# <td><%= link_to "Show", user, class:"btn btn-light btn-sm", style:"width: 100%; height: 100%;"%>
    <%# </td> %>
    <%# <td><%= link_to "Edit", edit_user_path(user), class:"btn btn-light btn-sm", style:"width: 100%; height: 100%;" %>
    <%# </td> %> 
    <%# <td><%= button_to "Destroy", user, method: :delete, class:"btn btn-light btn-sm", style:"width: 100%; height: 100%;" %>
    <%# </td>  %>
  </tr>
</tbody>